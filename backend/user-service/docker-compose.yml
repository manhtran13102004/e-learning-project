services:
  api:
    container_name: ev-user-service
    build:
      context: .
      dockerfile: Dockerfile
    image: atomi.digital.com/ev-services/user-service:v1
    restart: always
    ports:
      - 8083:8083
    volumes:
      - ./logs:/app/logs
